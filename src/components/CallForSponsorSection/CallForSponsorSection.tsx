import { component$ } from '@builder.io/qwik';

import css from './CallForSponsorSection.module.scss';

const CallForSponsorSection = component$(() => {
  return <section class={css.root}></section>;
});

export default CallForSponsorSection;
